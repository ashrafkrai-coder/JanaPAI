{
  "name": "janapai",
  "version": "1.0.0",
  "private": true,
  "description": "Penjana Soalan Pendidikan Islam KSSM Tingkatan 1-5 & Penjana RPT (PWA + Supabase + Gemini)",
  "scripts": {
    "dev:web": "npx --yes serve web -l 5173",
    "icons": "node scripts/make-icons.mjs",
    "db:push": "supabase db push",
    "functions:deploy": "supabase functions deploy masuk data jana-soalan jana-rpt jana-spm",
    "secrets:set": "supabase secrets set --env-file supabase/functions/.env",
    "percubaan": "node scripts/import-percubaan.mjs",
    "check": "cd supabase/functions && deno check */index.ts"
  },
  "devDependencies": {
    "supabase": "^2.0.0"
  }
}
