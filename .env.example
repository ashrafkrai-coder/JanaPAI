# Salin ke `.env` dan isi. `.env` diabaikan oleh git — JANGAN commit.
# Nhost Dashboard > Project Settings (subdomain, region) & Settings > Hasura (admin secret)
NHOST_SUBDOMAIN=
NHOST_REGION=
# Balut dengan petik tunggal — secret Nhost selalunya ada # atau $ yang akan memotong nilai.
NHOST_ADMIN_SECRET=''
